export function add(first, second) {
  return Number((first + second).toFixed(2));
}
export function subtract(first, second) {
  return Number((first - second).toFixed(2));
}

export function getFreePlaysForAmount(amount) {
  return amount / 0.2;
}
